# Use the Vite dev proxy (recommended for local dev — keeps auth cookies on the same origin)
# VITE_API_URL=/api
# VITE_SOCKET_URL=

# Direct backend URL (only if not using the Vite proxy)
# VITE_API_URL=http://localhost:5000/api
# VITE_SOCKET_URL=http://localhost:5000
